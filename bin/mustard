#!/usr/bin/env ruby

begin
  require 'mustard/main'
rescue LoadError
  $: << File.expand_path( File.join( File.dirname( __FILE__ ), %w[ .. lib ] ) ) 
  require 'mustard/main'
end
::Mustard::Main.run( ARGV )
